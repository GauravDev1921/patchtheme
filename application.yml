server: # REST and WS server
  port: 2333
  address: 0.0.0.0
  http2:
    enabled: false # Whether to enable HTTP/2 support
plugins:
  youtube:
    enabled: true # Whether this source can be used.
    allowSearch: true # Whether "ytsearch:" and "ytmsearch:" can be used.
    allowDirectVideoIds: false # Whether just video IDs can match. If false, only complete URLs will be loaded.
    allowDirectPlaylistIds: false # Whether just playlist IDs can match. If false, only complete URLs will be loaded.
    # The clients to use for track loading. See below for a list of valid clients.
    # Clients are queried in the order they are given (left-to-right)
    #oauth:
    # setting "enabled: true" is the bare minimum to get OAuth working.
    #enabled: true
    #refreshToken: "1//0gASVtpKU3KrrCgYIARAAGBASNgF-L9IrWzP8EYfPvZxz-iBRk0MHjIsyqDNsB0NVA-F98Ljfq7V9ynghyNnrGaOYSKQDxYSrbg"
    #skipInitialization: true
    pot:
      token: "MnTD0UiP-TrbdMPpyqHYBy1v3UxXzV84kBEunNy6FV9GUXzWiNp4mM2Wh43oxEdkmV-1sGYLKuVPfecENFNtzzVvXZZ529GZx1vqZaGFVgWQuuKIgGvGeEyNpmi4Knc0VoENpcXASesnm74yhsoXCyiUO_ucYw=="
      visitorData: "CgtkR2hxblFpWkR2ayiyyJe2BjIKCgJJThIEGgAgJQ%3D%3D"
    clients:
      - MUSIC
      - TVHTML5EMBEDDED
      - WEB
      - ANDROID_TESTSUITE
      - ANDROID_MUSIC

    # You can configure individual clients with the following.
    # Any options or clients left unspecified will use their default values
    # for those individual clients.
    # Client configurations will ONLY take effect if the client is registered above,
    # otherwise they are ignored.
    # ---------------- WARNING ----------------
    # This part of the config is for DEMONSTRATION PURPOSES ONLY!
    # Do NOT use this config before understanding what the options do.
    # You do NOT need to copy this config just because it is published here.
    # ---------------- WARNING ----------------
    WEB: # names are specified as they are written below under "Available Clients".
      # This will disable using the WEB client for video playback.
      playback: true
      searching: true
      playlistLoading: true
      videoLoading: true
    ANDROID_TESTSUITE:
      searching: false
    ANDROID_MUSIC:
      searching: true
      playlistLoading: true
      playback: true
    TVHTML5EMBEDDED:
      # The below config disables everything except playback for this client.
      playlistLoading: true
      playback: true # Disables loading of playlists and mixes for this client.
      videoLoading: true # Disables loading of videos for this client (playback is still allowed).
      searching: true # Disables the ability to search for videos for this client.
  lavalyrics:
    # sources is used to sort the different lyrics sources by priority (from highest to lowest)
    sources:
      - youtube
      - spotify
  lavasrc:
    providers:
      - 'ytsearch:"%ISRC%"' # Will be ignored if track does not have an ISRC. See https://en.wikipedia.org/wiki/International_Standard_Recording_Code
      - "ytsearch:%QUERY%" # Will be used if track has no ISRC or no track could be found for the ISRC
      - "scsearch:%QUERY%"
      - 'ytmsearch:"%ISRC%"'
      #  you can add multiple other fallback sources here
    sources:
      spotify: true # Enable Spotify source
      youtube: true # Enable YouTube search source (https://github.com/topi314/LavaSearch)
    lyrics-sources:
      spotify: true # Enable Spotify lyrics source
      youtube: true # Enable YouTube lyrics source
    spotify:
      clientId: "7244f5ef8ce64b5aa1b420f9c67df0f4"
      clientSecret: "7fd5be920b034550ac415f3ba50cf569"
      spDc: "AQDmpMpqejYhLHE6VijElbVjwEkeUG8-AS3bY_se8XIRNYoPH87heS8l_2PujT06QKKmAjywf-amUIsU_94DLolUj8ctfosYHmgyJ-D8IhWypKSFy_zYzaDEv5wNoWoCojQEITYa72yFz8RO9wJQCBoZ2fmZfYNlVSTRELuqyb5qYspV7PkTmq97r0ieFjP9NPN4BGYRzVOdBcWg-dDxuYNAEZw" # the sp dc cookie used for accessing the spotify lyrics api
      countryCode: "IN" # the country code you want to use for filtering the artists top tracks. See https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2
      playlistLoadLimit: 30 # The number of pages at 100 tracks each
      albumLoadLimit: 30 # The number of pages at 50 tracks each
    youtube:
      countryCode: "IN" # the country code you want to use for searching lyrics via ISRC. See https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2

#  name: # Name of the plugin
#    some_key: some_value # Some key-value pair for the plugin
#    another_key: another_value
lavalink:
  plugins:
    - dependency: "me.duncte123:java-lyrics-plugin:1.6.4"
    - dependency: "dev.lavalink.youtube:youtube-plugin:1.8.3"
    - dependency: "com.github.topi314.lavasrc:lavasrc-plugin:4.3.0"
    - dependency: "com.github.topi314.lavasearch:lavasearch-plugin:1.0.0"
      repository: "https://maven.lavalink.dev/releases" # this is optional for lavalink v4.0.0-beta.5 or greater
      snapshot: false
  #    - dependency: "com.github.example:example-plugin:1.0.0" # required, the coordinates of your plugin
  #      repository: "https://maven.example.com/releases" # optional, defaults to the Lavalink releases repository by default
  #      snapshot: false # optional, defaults to false, used to tell Lavalink to use the snapshot repository instead of the release repository
  #  pluginsDir: "./plugins" # optional, defaults to "./plugins"
  #  defaultPluginRepository: "https://maven.lavalink.dev/releases" # optional, defaults to the Lavalink release repository
  #  defaultPluginSnapshotRepository: "https://maven.lavalink.dev/snapshots" # optional, defaults to the Lavalink snapshot repository
  server:
    password: "viounbeatable"
    sources:
      # The default YouTube source is now deprecated and won't receive further updates. Please use https://github.com/lavalink-devs/youtube-source#plugin instead.
      youtube: false
      bandcamp: true
      soundcloud: true
      twitch: false
      vimeo: true
      nico: true
      http: true # warning: keeping HTTP enabled without a proxy configured could expose your server's IP address.
      local: true
    filters: # All filters are enabled by default
      volume: true
      equalizer: true
      karaoke: true
      timescale: true
      tremolo: true
      vibrato: true
      distortion: true
      rotation: true
      channelMix: true
      lowPass: true
    bufferDurationMs: 600 # The duration of the NAS buffer. Higher values fare better against longer GC pauses. Duration <= 0 to disable JDA-NAS. Minimum of 40ms, lower values may introduce pauses.
    frameBufferDurationMs: 5000 # How many milliseconds of audio to keep buffered
    opusEncodingQuality: 10 # Opus encoder quality. Valid values range from 0 to 10, where 10 is best quality but is the most expensive on the CPU.
    resamplingQuality: LOW # Quality of resampling operations. Valid values are LOW, MEDIUM and HIGH, where HIGH uses the most CPU.
    trackStuckThresholdMs: 10000 # The threshold for how long a track can be stuck. A track is stuck if does not return any audio data.
    useSeekGhosting: true # Seek ghosting is the effect where whilst a seek is in progress, the audio buffer is read from until empty, or until seek is ready.
    youtubePlaylistLoadLimit: 6 # Number of pages at 100 each
    playerUpdateInterval: 5 # How frequently to send player updates to clients, in seconds
    youtubeSearchEnabled: true
    soundcloudSearchEnabled: true
    gc-warnings: true
    #ratelimit:
    #ipBlocks: ["2001:470:734c::/48"] # list of ip blocks
    #excludedIps: ["...", "..."] # ips which should be explicit excluded from usage by lavalink
    #strategy: "RotatingNanoSwitch" # RotateOnBan | LoadBalance | NanoSwitch | RotatingNanoSwitch
    #searchTriggersFail: true # Whether a search 429 should trigger marking the ip as failing
    #retryLimit: -1 # -1 = use default lavaplayer value | 0 = infinity | >0 = retry will happen this numbers times
    #youtubeConfig: # Required for avoiding all age restrictions by YouTube, some restricted videos still can be played without.
    #email: "ashtonhuh7@gmail.com" # Email of Google account
    #password: "Md@12345" # Password of Google account
    #httpConfig: # Useful for blocking bad-actors from ip-grabbing your music node and attacking it, this way only the http proxy will be attacked
    #  proxyHost: "localhost" # Hostname of the proxy, (ip or domain)
    #  proxyPort: 3128 # Proxy port, 3128 is the default for squidProxy
    #  proxyUser: "" # Optional user for basic authentication fields, leave blank if you don't use basic auth
    #  proxyPassword: "" # Password for basic authentication

metrics:
  prometheus:
    enabled: false
    endpoint: /metrics

sentry:
  dsn: ""
  environment: ""
#  tags:
#    some_key: some_value
#    another_key: another_value

logging:
  file:
    path: ./logs/

  level:
    root: INFO
    lavalink: DEBUG
    lavalink.server.io.SocketContext: TRACE
    com.sedmelluq.discord.lavaplayer.tools.ExceptionTools: DEBUG
    dev.lavalink.youtube.http.YoutubeOauth2Handler: INFO
    dev.lavalink.youtube: DEBUG

  request:
    enabled: true
    includeClientInfo: true
    includeHeaders: false
    includeQueryString: true
    includePayload: true
    maxPayloadLength: 10000

  logback:
    rollingpolicy:
      max-file-size: 1GB
      max-history: 30
